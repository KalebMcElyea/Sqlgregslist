-- use gregslist;

--  CREATE TABLE car
--  (
--    id INT AUTO_INCREMENT,
--    model VARCHAR(255) NOT NULL UNIQUE,
--    miles INT,
--    year INT,
--    price DECIMAL(8 , 2) NOT NULL,
--    PRIMARY KEY (id)
--  ); 

-- NOTE this is creating actual car

--  INSERT INTO car
-- (model, miles, price, year)
-- VALUES
--  ("Kia Forte", 10000, 75000, 2012);

-- Get All of a collection
--  SELECT * FROM car;

-- Get a specific from a collection
-- SELECT * FROM car WHERE model="Volkswagen" OR id=1;

-- DELETE FROM car WHERE id=1;

 -- DELETE car;
 -- DROP TABLE car;
-- TRUNCATE car;